# 暑假英文學習計畫 — 操作手冊（Operation Manual）

> 給維護者：日常怎麼改內容、重生發音、改版、重新部署，以及這次「發音升級」是怎麼用多 Agent 派工完成的。
> 對應版本：**20260812-1**。搭配：`重建指南.md`（從零建置）、`使用說明書.md`（給學生/家長）。

專案根：`C:\OpenCode\202606\summer-english-app`
線上站：Firebase `https://summer-goku.web.app`｜GitHub Pages `https://shanksbroly8964-oss.github.io/summer-english-practice/`
Firebase 專案：`goku-46e66`（Hosting 站台 `summer-goku`；此專案另有番茄鐘/JLPT/GEPT/TOEIC 四站，**勿覆蓋**）。

---

## 1. 改版三步驟（最重要，務必遵守）

任何動到前端（js/css/html/資料）後：

1. **bump 版本號**（否則使用者吃到舊快取）：
   - `index.html` 的 `window.APP_VERSION`（例：`20260812-1` → `20260812-2`）
   - `sw.js` 的 `CACHE_VERSION`（例：`summer-en-v5` → `summer-en-v6`）
2. **部署兩站**（見第 4 節）。
3. 使用者端：第一次可能要下拉重新整理一次；PWA 版會自動抓新版（controllerchange 自動重載）。

> 慣例：選擇題 `answer` 一律存「選項全文」；`[hidden]{display:none!important}` 已在 css 兜底；發音鈕優先 MP3、抓不到退回瀏覽器朗讀。

---

## 2. 改「內容」（單字 / 文法 / 文章 / 寫作）

內容都在 `data/*.json`：

| 想改什麼 | 改哪個檔 |
|---------|---------|
| 某週單字 | `data/vocab_w<週>.json`（每天 `words` 陣列，每個字 `{id,en,kk,pos,zh,ex,ex_zh}`） |
| 文法 | `data/grammar.json`（16 個；每個含 examples、quiz） |
| 文章 | `data/reading_w<週>.json`（每篇 `{id,date,title,text,questions[3]}`） |
| 寫作 | `data/writing.json` |
| 週主題/文法對應 | `data/plan.json` |

改完務必：

```bash
# 1) 驗證資料完整性（日期覆蓋、answer 全文在 options、寫作 target 在當週單字…）
node validate.js
# 2) 若改到 en / ex / examples / model_en / 文章 text，要重生對應發音
python tools/gen_audio.py --concurrency 12   # 會跳過沒變的、只補新/缺的
# 3) bump 版本號 + 部署（見第 1、4 節）
```

> ⚠️ 若你**改了某個字的英文或例句**，舊 mp3 檔名不變、內容卻過期。此時先刪掉該字對應的舊 mp3（`audio/word/<id>_*.mp3`、`audio/ex/<id>_*.mp3`）再跑生成器，才會重錄。

---

## 3. 發音 MP3 生成器 `tools/gen_audio.py`

```bash
python tools/gen_audio.py --manifest-only      # 只更新 data/audio-manifest.json（秒級）
python tools/gen_audio.py                       # 生成全部（跳過已存在，可續跑）
python tools/gen_audio.py --limit 20            # 只生成前 20 個（煙霧測試）
python tools/gen_audio.py --concurrency 12      # 併發數（預設 10）
```

- 語音 `en-US-AriaNeural`；正常 `+0%`、慢速 `-25%`。要換聲音/速度改檔案最上方常數。
- 命名：`audio/word/<id>_n.mp3`(_s 慢速)、`audio/ex/<id>_n.mp3`、`audio/grammar/<gId>_e<idx>_n.mp3`、`audio/writing/<週>-<idx>_n.mp3`、`audio/reading/<文章id>_s<句序>.mp3`（文章只正常速）。
- 前端契約在 `docs/AUDIO_CONTRACT.md`；改命名規則要同步改 `js/audio.js` 的 `src()`。

---

## 4. 重新部署

```bash
# 先確保工作區乾淨、版本號已 bump
cd "C:/OpenCode/202606/summer-english-app"
git add -A && git commit -m "..."

# Firebase（只部署 summer-goku，勿動其他四站）
firebase deploy --only hosting:summer-goku --project goku-46e66

# GitHub Pages（靠 git push）
git push origin main
```

驗證：
```bash
curl -s https://summer-goku.web.app/ | grep APP_VERSION
curl -s -o /dev/null -w "%{http_code}\n" https://summer-goku.web.app/audio/word/w1d1a_n.mp3
# 確認四站未受影響
for u in goku-46e66 jlpt-goku gept-goku toeic-goku; do curl -s -o /dev/null -w "$u %{http_code}\n" https://$u.web.app/; done
```

> `firebase.json` 已把 `tools/ docs/ tests/ rdq/ _prompts/ deliverables/ *.md` 排除，不會上 CDN；GitHub Pages 會含 `.md`（無妨）。

---

## 5. 發音功能架構（維護時要懂的最少知識）

- **SEAudio（`js/audio.js`）**：唯一播放入口。`play({scope,id,idx?,speed?,fallbackText,btnEl})` 優先播 MP3，失敗→ `SETts.speak` 備援。`playSequence(items,{onItem,onDone})` 做連播/逐句。`init()` 在 `app.js` 啟動時呼叫，載入 `data/audio-manifest.json`。
- **發音鈕**：`views.js` 的 `audioBtn(scope,id,idx,fallbackText)` 產生帶 `data-audio-*` 的鈕；`app.js` 有全域點擊代理呼叫 `SEAudio.play`。舊的 `[data-tts]` 鈕仍走瀏覽器朗讀（相容）。
- **聽力題**：`js/listening-quiz.js` 的 `SEListening.makeQuestions(words,n)` 產題；`quiz.js` 在每日測驗插 3 題、渲染「🔊 再聽一次」並進題自動播、答錯進錯題本（`k` 前綴 `l:`、`t='listening'`）。
- **逐句跟讀**：`js/reading-follow.js` 的 `SEReadingFollow.render(container, articleId)`；`views.js` 的文章頁掛 `.rf-mount` 容器並呼叫它。高亮用 `.rf-active`。
- **語速切換**：`SEStore.getSettings().ttsNormal`（true=正常/'n'、false=慢速/'s'）；`SEAudio.currentSpeed()` 與備援 rate 都讀它。

---

## 6. 這次「發音升級」的多 Agent 派工流程（可複用）

這次升級是用「主控（Claude Code）＋ OpenCode 多 Agent ＋ QA ＋ PM」完成的，流程可複用於下次大改：

1. **需求**：先用 RDQ 訪談確認範圍（規格卡在 `rdq/`）。
2. **A0（主控自己做）**：定 `docs/AUDIO_CONTRACT.md` 契約 + 寫 `tools/gen_audio.py` + 生成全部 MP3 + `data/audio-manifest.json`。音檔生成是確定性工作，主控自己跑最穩。
3. **派工（OpenCode）**：模型 `opencode-go/minimax-m3`（deepseek 系列當時餘額不足）。四個開發 Agent 各**只改自己的新檔**（零耦合、靠契約溝通）：
   ```bash
   opencode run -m opencode-go/minimax-m3 --dangerously-skip-permissions \
     --dir "C:/OpenCode/202606/summer-english-app" "$(cat _prompts/task_A1.txt)" > _logs/A1.log 2>&1 &
   ```
   A1 `js/audio.js`、A2 `js/listening-quiz.js`、A3 `js/reading-follow.js`、A4 `tests/`。
4. **完成判定**：看 `deliverables/A?.DONE` / `.BLOCKED` 檔出現，**不要只看 log 停不停**。
5. **看門狗**：由**主控**掛 `run_in_background` 的 until-loop 等 DONE（PM sub-agent 的背景迴圈會死，不可靠）。
6. **QA**（OpenCode）：靜態＋邏輯層品管，寫 `deliverables/QA_REPORT.md`。**真實瀏覽器 E2E 由主控自己用預覽工具做**（禁止 agent 開瀏覽器，會卡死）。
7. **PM**（Claude sub-agent）：稽核「有沒有照契約邊界執行」（用 `git status` 比對是否越界），寫 `deliverables/PM_REPORT.md`。
8. **整合**：**主控**改共用檔（index.html/views.js/quiz.js/app.js/css）把各模組接起來——整合期間才動共用檔，避免污染 PM 的邊界稽核。
9. **部署 + 驗收**：見第 4 節。

> 派工細節與教訓（模型分配、看門狗、禁止項）沉澱在使用者的記憶檔與 `docs/AUDIO_CONTRACT.md`。

---

## 7. 常用檢查

```bash
node validate.js                 # 內容資料完整性
node tests/run-tests.js          # 發音模組測試（38 條）
for f in js/*.js; do node --check "$f"; done   # 全部語法
find audio -name '*.mp3' | wc -l # 應為 2458
```
