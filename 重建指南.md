# 暑假英文學習計畫 — 重建指南（Rebuild Guide）

> 這份文件讓你（開發者）從零把「暑假英文學習計畫」這個 App 重建、部署到自己的網址。
> 對應版本：**20260812-1**（含 edge-tts 真人級發音 MP3）。
> 搭配文件：`操作手冊.md`（日常維護／改內容／重新部署）、`使用說明書.md`（給學生/家長）。

---

## 0. 這是什麼

- 一個**純前端**（vanilla JS，無框架、無打包）的 PWA 英文自學工具。
- 首頁是行事曆，8 週 56 天，每天 6 單字＋文法＋長文閱讀＋測驗；含錯題本、Google 登入雲端備份。
- **發音走預先生成的真人級英語 MP3**（edge-tts，en-US-AriaNeural），抓不到才退回瀏覽器即時朗讀。
- 可安裝到手機主畫面（PWA），可離線複習。

**技術棧**：HTML + CSS + 原生 JS ＋ Firebase（Auth + Firestore + Hosting）＋ Service Worker。發音 MP3 用 Python 的 `edge-tts` 在**本機一次生成**成靜態檔（使用者端不呼叫任何語音服務）。

---

## 1. 需要先裝的工具

| 工具 | 用途 | 檢查指令 |
|------|------|---------|
| Git | 取得原始碼、部署 GitHub Pages | `git --version` |
| Node.js（建議 v18+） | `node --check` 語法驗證、跑測試 | `node --version` |
| Python 3（建議 3.9+） | 生成發音 MP3 | `python --version` |
| edge-tts（Python 套件） | 真人級英語 TTS | `pip install edge-tts` → `edge-tts --version` |
| ffmpeg（選用） | 檢查/轉檔 MP3 | `ffmpeg -version` |
| firebase-tools | 部署 Firebase Hosting | `npm i -g firebase-tools` → `firebase --version` |
| GitHub CLI（選用） | 建立 repo、開 Pages | `gh --version` |

---

## 2. 取得原始碼

```bash
git clone https://github.com/shanksbroly8964-oss/summer-english-practice.git
cd summer-english-practice
```

- **音檔（audio/ 底下 2,458 個 mp3、約 50MB）已包含在 repo 內**，clone 完就能直接跑，不必重生。
- 若要自己重生音檔，見第 5 節。

---

## 3. 專案結構

```
summer-english-app/
├─ index.html                # 入口；APP_VERSION 版本號、PWA <head>、登入前門、載入各 js
├─ manifest.webmanifest      # PWA 設定（可安裝、圖示、主題色）
├─ sw.js                     # Service Worker（快取版本控管、離線）
├─ firebase.json             # Firebase Hosting 設定（站台、快取標頭、部署 ignore）
├─ css/style.css             # 全站樣式（含發音高亮、連播、聽力題）
├─ js/
│  ├─ firebase-config.js     # ★ Firebase 專案設定值（換成你自己的）
│  ├─ tts.js                 # 瀏覽器即時朗讀（Web Speech；備援用）
│  ├─ audio.js               # SEAudio 發音引擎（MP3 為主＋SETts 備援＋連播）
│  ├─ listening-quiz.js      # SEListening 聽音選字題
│  ├─ reading-follow.js      # SEReadingFollow 文章逐句高亮跟讀
│  ├─ storage.js             # localStorage + 雲端合併
│  ├─ data-loader.js         # 載入 data/*.json、日期工具
│  ├─ quiz.js                # 測驗引擎（含聽力題、錯題重練）
│  ├─ views.js              # 各頁面渲染
│  ├─ app.js                 # hash 路由 + 發音鈕全域代理 + SEAudio.init
│  ├─ auth.js                # Google 登入 + Firestore 同步 + 登入前門
│  └─ pwa.js                 # SW 註冊、更新提示、安裝引導
├─ data/
│  ├─ plan.json              # 8 週主題與文法對應
│  ├─ vocab_w1..w8.json      # 單字（每天 6 個）
│  ├─ reading_w1..w8.json    # 文章（每天 1 篇＋3 題）
│  ├─ grammar.json           # 16 個文法點
│  ├─ writing.json           # 8 週寫作
│  └─ audio-manifest.json    # ★ 內容→MP3 路徑對照表（由 tools/gen_audio.py 產）
├─ audio/                    # ★ 2,458 個發音 mp3（word/ex/grammar/writing/reading）
├─ icons/                    # PWA 圖示（svg + png）
├─ tools/gen_audio.py        # 發音 MP3 生成器（edge-tts）
├─ tests/run-tests.js        # Node 測試（38 條）
├─ docs/AUDIO_CONTRACT.md    # 發音模組契約
└─ validate.js               # 內容資料驗證
```

---

## 4. 建立你自己的 Firebase 專案（登入＋雲端備份需要）

> 若你**只想先看到 App 跑起來、不需要登入/雲端**，可跳過本節：登入前門會顯示「先不登入，直接使用」，純本機也能用。

1. 到 <https://console.firebase.google.com> 建一個新專案。
2. 專案內「Authentication → Sign-in method」啟用 **Google** 供應商。
3. 「Build → Firestore Database」建立資料庫（正式模式）。到「規則」貼上：
   ```
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /users/{uid}/{document=**} {
         allow read, write: if request.auth != null && request.auth.uid == uid;
       }
     }
   }
   ```
   （只有本人能讀寫自己的資料。）
4. 「專案設定 → 一般 → 你的應用程式 → 新增 Web App」，取得設定值，填進 `js/firebase-config.js`：
   ```js
   var firebaseConfig = {
     apiKey: "…", authDomain: "<專案>.firebaseapp.com",
     projectId: "<專案>", storageBucket: "<專案>.firebasestorage.app",
     messagingSenderId: "…", appId: "…"
   };
   ```
   （這些是**公開值**，可以放前端。）
5. 部署後，把你的網域加入 Authentication 的「授權網域」（見第 6 節末）。

---

## 5. （選用）重新生成發音 MP3

音檔已在 repo；只有在你**改了單字/文法/文章內容**時才需要重生對應音檔。

```bash
# 只更新對照表（秒級）
python tools/gen_audio.py --manifest-only
# 生成全部（會跳過已存在的檔，可續跑）
python tools/gen_audio.py --concurrency 12
```

- 語音、速度、命名規則都寫在 `tools/gen_audio.py` 最上方（en-US-AriaNeural；正常 `+0%`、慢速 `-25%`）。
- 生成完 `data/audio-manifest.json` 會一起更新。
- 驗證內容資料完整性：`node validate.js`；驗證發音模組：`node tests/run-tests.js`。

---

## 6. 部署

### A. Firebase Hosting

1. 登入：`firebase login`
2. 在 Firebase Console 建一個 Hosting 站台（例如 `my-summer`），記下網址 `my-summer.web.app`。
3. 改 `firebase.json` 的 `"site"` 為你的站台名（`my-summer`）。
4. 部署：
   ```bash
   firebase deploy --only hosting:my-summer --project <你的專案ID>
   ```
5. 完成後把 `my-summer.web.app` 與 `my-summer.firebaseapp.com` 加入 Authentication →「授權網域」，Google 登入才會過。

> `firebase.json` 已設定：`*.html`、`sw.js`、`manifest` 不快取（確保更新即時）；`_prompts/`、`docs/`、`tests/`、`tools/`、`*.md` 等開發檔不部署上 CDN。

### B. GitHub Pages

```bash
# 若還沒有 repo：
gh repo create summer-english-practice --public --source . --push
# 開啟 Pages（main 分支根目錄）：
gh api repos/<你的帳號>/summer-english-practice/pages -X POST -f "source[branch]=main" -f "source[path]=/"
```
- 已含 `.nojekyll`（避免 Jekyll 略過底線開頭資料夾）。
- 網址：`https://<你的帳號>.github.io/summer-english-practice/`。
- 也把這個網域加入 Firebase 授權網域，登入才會過。

---

## 7. 驗收清單

- [ ] 打開網址，登入前門正常（可「先不登入直接使用」）。
- [ ] 點單字/例句/文法/寫作的 🔊 → 聽到**真人發音**（開發者工具 Network 看到 `audio/....mp3` 回 200/206）。
- [ ] 語速切「慢速/正常」→ 下一次播放換另一組音檔。
- [ ] 單字頁「▶ 連續播放整課」會依序播、卡片高亮。
- [ ] 文章頁「▶ 逐句播放」逐句高亮跟讀，可點句跳播。
- [ ] 每日測驗出現「聽力」題（🔊 再聽一次），答錯進錯題本、錯題本可重練。
- [ ] 手機可「加入主畫面」、離線可開已看過的頁。
- [ ] Google 登入後重整仍保持登入；換裝置同帳號紀錄還原。

---

## 8. 疑難排解

| 症狀 | 原因 / 解法 |
|------|------------|
| 🔊 沒聲音、但沒報錯 | 手機靜音鍵、或首次需先點畫面一次才有聲音（瀏覽器自動播放限制）。MP3 抓不到會自動退回瀏覽器朗讀。 |
| 改了程式手機還是舊版 | 沒 bump 版本號。改前端要同時把 `index.html` 的 `APP_VERSION` 與 `sw.js` 的 `CACHE_VERSION` +1（見 `操作手冊.md`）。 |
| Google 登入彈窗閃退/unauthorized-domain | 網域沒加進 Firebase 授權網域。 |
| Pages 顯示舊版 | Pages 有建置延遲；加 `?cb=時間戳` 破快取確認。 |
| 音檔 404 | 該內容還沒生成對應 mp3；跑 `python tools/gen_audio.py`。 |
